extends ../../layout/homepage-layout

block content
  h1= title
  if imamgine != null
    mixin immagine(immagine)

  else
    if list_immagini != null
      each img in list_immagini
        mixin immagine(img)
